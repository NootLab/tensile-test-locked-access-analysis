When Access database files are locked (password-protected, read-only, or in-use), 
you can view the data but cannot export it directly. This tool lets you:
1. Copy column data in manageable chunks
2. Paste into the GUI
3. Process and visualize without needing database access
